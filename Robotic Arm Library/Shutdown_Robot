function Shutdown_Robot( obj )

%Shutdown Robot arm, Finalize shutdown procedure by releaseing the COM port 
fprintf(obj,'shutdown'); %Send the signal to the Robot Controller

fclose(obj); %Release Robot Controller object in matlab

obj %Print result

end
